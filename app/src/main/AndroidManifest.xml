<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.har.quickrepairforandroid">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".Service.PollOrderService"
            android:exported="false"
            android:process=".PollOrderService"/>

        <activity android:name=".UI.AccountDetailActivity" />
        <activity android:name=".UI.OrderDetailActivity" />
        <activity android:name=".UI.RegisterServiceActivity" />
        <activity android:name=".UI.AddOrderActivity" />
        <activity
            android:name=".UI.NavigationActivity"
            android:label="@string/title_activity_navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.LoginActivity"
            android:label="@string/title_activity_login"
            android:parentActivityName=".UI.NavigationActivity" />
    </application>

</manifest>